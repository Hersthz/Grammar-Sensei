/**
 * Grammar Sensei – Popup Script
 *
 * Runs inside popup.html when the user clicks the extension icon.
 * Currently displays static info; can be extended to show history
 * or settings in future versions.
 */

document.addEventListener("DOMContentLoaded", () => {
  /* Future: load stats from chrome.storage */
  console.log("Grammar Sensei popup loaded.");
});
